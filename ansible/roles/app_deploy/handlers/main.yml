---
- name: Reload systemd
  ansible.builtin.systemd:
    daemon_reload: true

- name: Restart secure-api
  ansible.builtin.systemd:
    name: secure-api
    state: restarted

- name: Reload nginx
  ansible.builtin.service:
    name: nginx
    state: reloaded
